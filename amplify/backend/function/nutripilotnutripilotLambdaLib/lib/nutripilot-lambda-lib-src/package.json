{
  "name": "nutripilot-lambda-lib",
  "version": "1.0.0",
  "private": true,
  "description": "Shared DynamoDB client and macro calculation helpers for NutriPilot Lambda functions.",
  "//": "files[] is required: npm pack writes its .tgz into this directory, so without an explicit allowlist the next pack bundles the previous tarball into itself.",
  "files": [
    "calculateMacros.js",
    "dynamoClient.js"
  ]
}
